name: 'Test coverage'
on:
  workflow_dispatch:

jobs:
  test_env_setup_job:
    runs-on: ubuntu-latest
    name: Test coverage
    steps:
      - name: Checkout [master]
        uses: actions/checkout@v3
        
      - name: Checkout [react-charting]
        uses: actions/checkout@v4
        with: 
          repository: "microsoft/fluentui"
          path: repo2

      - name: Install packages
        run: yarn --cwd ./tools/UnitTestApp --verbose   

      - name: Test Environment Setup action step
        id: setup
        uses: ./tools/UnitTestApp/TestSetup
        
      - name: Run coverage report
        run: yarn test --coverage --collectCoverageFrom='../../repo2/packages/react-charting/src/components/Sparkline'
        working-directory: ./tools/UnitTestApp
        
