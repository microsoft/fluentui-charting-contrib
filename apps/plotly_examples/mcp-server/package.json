{
  "name": "plotly-dataset-mcp",
  "version": "1.0.0",
  "description": "MCP server to automate Plotly dataset generations.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "build": "tsc",
    "test": "npm run build && node --test tests/security.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.16.0",
    "express": "^5.1.0",
    "puppeteer": "^21.11.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "ts-node": "^10.0.0",
    "typescript": "^5.6.3"
  }
}